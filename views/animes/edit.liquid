{% layout 'layout.liquid' %}
{% block content %}

  <div class="container-sm">
    {%- comment -%}
      change the action
    {%- endcomment -%}
    {%- comment -%}
      add values to input fields
    {%- endcomment -%}
    {%- comment -%}
      add a back button
    {%- endcomment -%}
    <form action="/animes/{{anime.id}}?_method=PUT" method="POST">
      <fieldset>
        <legend>Edit
          {{anime.name}}
        </legend>
        <label class="form-label">Name:
        </label>
        <input class="form-control" type="text" name="name" placeholder="enter the name of the character" value="{{ anime.name }}">
        <label class="form-label">anime:
        </label>
        <input class="form-control" type="text" name="anime" placeholder="what anime is he in?" value="{{ anime.anime }}">
        <label class="form-label">Does He Solo:
        </label>
        <input class="form-check-input" type="checkbox" name="doesHeSolo" {% if anime.doesHeSolo %}checked
          {% endif %}></fieldset>
      <input class="btn btn-success" type="submit" value="Edit {{anime.name}}">
    </form>
  </div>

{% endblock %}}}}}}}